# Intake Evaluation: Issue  — Revive pattern: mirror-architecture

> **Date:** 2026-03-15
> **Issue:** 
> **Evaluation Result:** Pattern revival — Tier 2 (Project Spec exists: PROJECT-30)

## Tier Assessment

**Tier 2 — Project Spec already exists.** PROJECT-30 (Mirror Data Architecture) is active with 16 items across data-pr, semops-research, and publisher-pr. This issue is pattern registration work — un-retiring a prematurely retired pattern. No new project spec needed.

## Territory Map

### What Exists

| Artifact | Location | State |
|----------|----------|-------|
| Pattern table row | `pattern.id = 'mirror-architecture'` | Present, `lifecycle_stage: retired` |
| Entity table row | `entity.id = 'mirror-architecture'` | Present, `entity_type: content`, `lifecycle_stage: active` (MISMATCH) |
| Pattern edge | `mirror-architecture --extends--> ddd` | Present (strength 1.00) |
| Entity edges | None | No capability or repo edges |
| Pattern doc (retired stub) | `semops-orchestrator/docs/domain-patterns/retired/mirror-architecture.md` | Exists |
| Pattern doc (active) | `semops-data/docs/domain-patterns/mirror-architecture.md` | DOES NOT EXIST |
| Registry.yaml | `config/registry.yaml` | NOT PRESENT (cleaned during retirement) |
| ADR-0004 | `semops-orchestrator/docs/decisions/ADR-0004-mirror-architecture.md` | Exists, ingested as KB entity |
| UL reference | `schemas/UBIQUITOUS_LANGUAGE.md` line 181 | Listed as `implementation` type example |
| Coherence analysis | `docs/analysis/mirror-architecture-reference.md` | Exists (updated with revival context) |
| PROJECT-30 spec | `semops-orchestrator/docs/project-specs/PROJECT-30-mirror-data-architecture.md` | Active, 16 items |
| Scale-projection pattern | `pattern.id = 'scale-projection'` | Active, pattern_type: concept |
| `synthetic-data` pattern | Not in pattern table | DOES NOT EXIST |

### Capability-Pattern Coverage (from PROJECT-30 spec)

| Capability | In DB? | In Registry? | Implements Patterns (current) | mirror-architecture gap? |
|---|---|---|---|---|
| `data-due-diligence` | Yes | Yes | `ddd`, `data-modeling`, `explicit-architecture`, `business-domain`, `togaf`, `dama-dmbok`, `dcam`, `apqc-pcf`, `data-system-classification` | Not mapped to mirror-architecture |
| `system-primitive-decomposition` | Yes | Yes | `explicit-architecture`, `explicit-enterprise`, `data-system-classification` | Not mapped to mirror-architecture |
| `synthesis-simulation` | Yes | Yes | `scale-projection`, `data-profiling`, `data-lineage`, `data-modeling` | Not mapped to mirror-architecture |
| `mirror-architecture` (as capability) | NO | NO | N/A | **Missing entirely** — listed in PROJECT-30 as a capability but never registered |

### Scale-Projection Relationship

Scale-projection is a **concept** pattern (infrastructure-up projection with RLHF/SECI/data-profiling). Mirror-architecture is an **infrastructure** pattern (the system-mirroring process). They share DDD foundations but address different problems:

- **Scale Projection:** "Project a domain model across infrastructure tiers to validate coherence at scale"
- **Mirror Architecture:** "Stand up a running mirror of an existing data system, measure the gap between reference model and reality"

Scale-projection edges: `extends→rlhf`, `extends→seci`, `extends→data-profiling`, `extends→ddd`
Mirror-architecture edges: `extends→ddd` (only)

## Delta (What's Missing)

### HIGH priority

| # | Gap | Action |
|---|-----|--------|
| 1 | Pattern `lifecycle_stage` = retired | Update pattern metadata: `lifecycle_stage: retired → development` |
| 2 | No pattern doc | Create `docs/domain-patterns/mirror-architecture.md` — describe the 4-step process (discover → populate → validate → overlay), relationship to scale-projection, PROJECT-30 binding |
| 3 | Not in registry.yaml | Add `mirror-architecture` as a pattern in capability `implements_patterns` lists. See capability mapping below |
| 4 | `mirror-architecture` capability not registered | Register as capability in registry.yaml (entity_type=capability, delivered_by data-pr + semops-research) |

### MEDIUM priority

| # | Gap | Action |
|---|-----|--------|
| 5 | SKOS edges incomplete | Add `related→scale-projection`, `related→explicit-architecture` pattern_edges |
| 6 | Capability-pattern wiring | Add `mirror-architecture` to `implements_patterns` for: `data-due-diligence`, `system-primitive-decomposition`, `synthesis-simulation` |
| 7 | Retired stub in semops-orchestrator | Update or redirect `semops-orchestrator/docs/domain-patterns/retired/mirror-architecture.md` |
| 8 | `synthetic-data` not a registered pattern | Register `synthetic-data` as 3P pattern (needed by `synthesis-simulation` — already in edge table but not pattern table) |

### LOW priority

| # | Gap | Action |
|---|-----|--------|
| 9 | Entity metadata lifecycle alignment | Entity row already says `active` — correct for revival, but `entity_type` should potentially be `capability` not `content` |
| 10 | publisher-pr draft references | Already correct for revived pattern — no action needed |
| 11 | ARCHITECTURE.md reference | Add mirror-architecture to pattern listing |

## Recommended Actions for Issue 

### Step 1: Pattern table update
```sql
UPDATE pattern SET metadata = jsonb_set(metadata, '{lifecycle_stage}', '"development"')
WHERE id = 'mirror-architecture';
```

### Step 2: Create pattern doc
Create `docs/domain-patterns/mirror-architecture.md` with:
- 4-step process: discover → populate → validate → overlay
- Distinction from scale-projection
- PROJECT-30 binding
- Provenance: 1P (synthesizes DDD, data-profiling, explicit-architecture)
- Pattern type: infrastructure

### Step 3: Registry.yaml updates
- Add `mirror-architecture` capability under `core.acquisition` (or new sub-section)
- Add `mirror-architecture` to `implements_patterns` for `data-due-diligence`, `system-primitive-decomposition`, `synthesis-simulation`

### Step 4: SKOS edges
- Add `mirror-architecture --related--> scale-projection`
- Add `mirror-architecture --related--> explicit-architecture`
- Keep existing `mirror-architecture --extends--> ddd`

### Step 5: Ingest
Run `ingest_architecture.py` after registry.yaml changes to propagate.

## Queries Executed

- `pattern` table: mirror-architecture (retired), scale-projection (active)
- `entity` table: mirror-architecture (content/active), related capabilities
- `pattern_edge`: mirror-architecture edges (1: extends→ddd), scale-projection edges (4)
- `edge` table: capability→pattern implements edges for related patterns
- Registry.yaml: full read — mirror-architecture absent
- PROJECT-30 spec: full read — 4 capabilities, 16 items, active
- Coherence analysis: full read — revival context already added
