# Intake Evaluation: Issue  — Register pattern: semantic-optimization

> **Date:** 2026-03-15
> **Issue:** 
> **Evaluator:** /intake (agent)

## Tier Assessment

**Result: Tier 1 (Single Issue)**

Promotion trigger check:
- Spans 3+ repos? **No** — registration is semops-data only (pattern table, pattern_edge, capability edge). Downstream repos consume but don't change.
- Requires ADR? **No** — ADR-0012 already provides the decision basis. No new architectural decision needed.
- Has blocking dependencies? **No** — both child patterns (`semantic-object-pattern`, `semantic-coherence`) are already registered in the pattern table.
- Needs sequenced execution? **No** — single-repo, single-pass registration.

This is a standard pattern registration task with well-scoped goals and pre-resolved identity.

---

## Territory Map

### What Exists

| Layer | Artifact | Status |
|-------|----------|--------|
| **Pattern table** | `semantic-object-pattern` (1P, domain) | REGISTERED |
| **Pattern table** | `semantic-coherence` (1P, domain) | REGISTERED |
| **Pattern table** | `semantic-optimization` | **ABSENT** (0 of 61 patterns) |
| **Pattern edges** | `semantic-coherence --[extends]--> ddd` | PRESENT |
| **Pattern edges** | `semantic-coherence --[extends]--> skos` | PRESENT |
| **Pattern edges** | `semantic-object-pattern --[extends]--> pattern-language` | PRESENT |
| **Pattern edges** | `semantic-optimization` broader edges | **ABSENT** |
| **Capability edge** | `coherence-scoring --[implements]--> semantic-coherence` | PRESENT |
| **Capability edge** | `coherence-scoring --[implements]--> semantic-optimization` | **ABSENT** |
| **Registry.yaml** | `coherence-scoring` lists `semantic-optimization` | PRESENT (line 193) |
| **Domain pattern doc** | `semantic-coherence.md` | PRESENT |
| **Domain pattern doc** | `semantic-object-pattern.md` | PRESENT |
| **Domain pattern doc** | `semantic-optimization.md` | **ABSENT** |
| **KB entities** | 7 entities with `primary_pattern_id = NULL` | PRESENT but UNLINKED |
| **Reference analysis** | `docs/analysis/semantic-optimization-reference.md` | PRESENT (comprehensive) |
| **ADR-0012** | Definition basis | PRESENT |
| **STRATEGIC_DDD.md** | 3 references | PRESENT |
| **ARCHITECTURE.md** | 0 mentions | **ABSENT** |
| **UL entry** | Referenced but no definition block | **PARTIAL** |

### Child Pattern Details (DB confirmed)

| Pattern | Preferred Label | Provenance | Type | SKOS Edges |
|---------|----------------|------------|------|------------|
| `semantic-object-pattern` | Semantic Object Pattern | 1P | domain | extends → `pattern-language` |
| `semantic-coherence` | Semantic Coherence | 1P | domain | extends → `ddd`, extends → `skos` |

---

## Delta: What's Missing

### Critical (blocks pattern coverage and fitness functions)

| # | Gap | Impact |
|---|-----|--------|
| 1 | **No pattern table row** for `semantic-optimization` | All downstream artifacts impossible — edges, coverage, fitness |
| 2 | **No SKOS `broader` edges** to children | Parent-child taxonomy not encoded |
| 3 | **No IMPLEMENTS edge** `coherence-scoring → semantic-optimization` | Registry/DB mismatch — registry.yaml declares it, DB cannot honor it |
| 4 | **7 entities with null `primary_pattern_id`** | Pattern has invisible content; coverage = 0 |

### Important (completeness)

| # | Gap | Impact |
|---|-----|--------|
| 5 | **No domain pattern doc** `docs/domain-patterns/semantic-optimization.md` | Pattern has no canonical documentation file |
| 6 | **No UL definition block** | Pattern referenced but not formally defined in domain vocabulary |
| 7 | **semantic-coherence.md** says "Derives From: semantic-optimization" | Label should say SKOS `narrower` — direction is correct, label is misleading |

### Low Priority (defer)

| # | Gap | Impact |
|---|-----|--------|
| 8 | ARCHITECTURE.md has 0 mentions | Low — STRATEGIC_DDD.md covers it |
| 9 | Legacy entities in docs-pr `_legacy/` | Cleanup, not blocking |
| 10 | DESCRIBED_BY edges from pattern to content entities | Meaningful only after pattern row exists |

---

## Capability-Pattern Coverage Table

Capabilities that reference `semantic-optimization` in registry.yaml:

| Capability | Domain | Status | Patterns (registry.yaml) | DB IMPLEMENTS edges | Delta |
|------------|--------|--------|--------------------------|---------------------|-------|
| `coherence-scoring` | core / measurement_and_memory | in_progress | `semantic-optimization`, `semantic-coherence` | `semantic-coherence` only | **Missing: `semantic-optimization`** |

After registration, the IMPLEMENTS edge `coherence-scoring → semantic-optimization` can be created, resolving the registry/DB mismatch.

No other capabilities in registry.yaml reference `semantic-optimization`.

---

## Recommended Actions

### Registration pass (issue  scope)

1. **Insert pattern row:**
   - `id`: `semantic-optimization`
   - `preferred_label`: `Semantic Optimization`
   - `definition`: "The control loop that minimizes the gap between pattern targets and coherence measurements. Pattern sets the target, Coherence measures the gap, Optimization is the loop that closes it." (basis: ADR-0012)
   - `provenance`: `1p`
   - `metadata`: `{"pattern_type": "concept", "$schema": "pattern_metadata_v1"}`

2. **Insert pattern_edge rows (SKOS broader):**
   - `semantic-optimization --[broader]--> semantic-object-pattern`
   - `semantic-optimization --[broader]--> semantic-coherence`

3. **Insert capability IMPLEMENTS edge:**
   - `coherence-scoring --[implements]--> semantic-optimization`

4. **Backfill `primary_pattern_id`** on relevant entities (at least `semantic-optimization`, `pub-semantic-optimization`, `semantic-optimization-implementation`)

5. **Create domain pattern doc** `docs/domain-patterns/semantic-optimization.md` following the template used by siblings

6. **Update `semantic-coherence.md` line 7** — change "Derives From: `semantic-optimization`" to use SKOS `narrower` label

### Out of scope (defer)

- UL definition block update
- ARCHITECTURE.md mention
- Legacy entity cleanup
- DESCRIBED_BY edges
- Fitness function coverage (needs coherence-scoring operational first)

---

## Evaluation Result

| Field | Value |
|-------|-------|
| **Tier** | 1 (Single Issue) |
| **Classification** | Pattern registration |
| **Complexity** | Low — all inputs resolved, children registered, definition from ADR-0012 |
| **Dependencies** | None blocking — children already exist |
| **Estimated actions** | 6 (insert pattern, 2 SKOS edges, 1 capability edge, backfill entities, create doc) |
| **Label applied** | `intake:evaluated` |
